twig:
    default_path: '%kernel.project_dir%/templates'
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'

framework:
    secret:     'secret'
    test: true
    router:     { resource: null, utf8: true }

fm_tinymce:
    instances:
        default:
          options: { }
